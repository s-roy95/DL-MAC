# Quick containerized demo: build the task-runner image and bring up four
# edge-server function instances sharing one /data volume. Each is the SAME
# image used on kind/k8s. Curl any of them, or run the experiment driver with
# --backend local (which manages its own task-runners) for the full pipeline.
services:
  es1: {build: ./task-runner, environment: [FUNC_TYPE=generic], volumes: ["shared:/data"], ports: ["8081:8080"]}
  es2: {build: ./task-runner, environment: [FUNC_TYPE=generic], volumes: ["shared:/data"], ports: ["8082:8080"]}
  es3: {build: ./task-runner, environment: [FUNC_TYPE=generic], volumes: ["shared:/data"], ports: ["8083:8080"]}
  gw:  {build: ./task-runner, environment: [FUNC_TYPE=generic], volumes: ["shared:/data"], ports: ["8084:8080"]}
volumes:
  shared:
